# 本地环境
ENV = development

# 本地环境接口地址
VITE_API_URL = http://localhost:8080

# WebSocket 地址（留空则使用当前页面 host，通过 vite proxy 代理）
# 如需直连后端（不使用代理），设置为 http://localhost:8080
VITE_WS_URL = http://localhost:8080